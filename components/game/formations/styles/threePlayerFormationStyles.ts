import { StyleSheet } from 'react-native';

export const threePlayerFormationStyles = StyleSheet.create({
  container: {
    flex: 1,
    position: 'relative',
    width: '100%',
    height: '100%',
  },
});